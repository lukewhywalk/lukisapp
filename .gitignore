# OS cruft
.DS_Store
Thumbs.db
desktop.ini

# Local tooling (preview server, launch config) -- not part of the app
.claude/

# Rules mit echten E-Mail-Adressen und exportierte Flugdaten -- nie committen
firestore.rules.local
migration/*.json
# Empfaengerdaten fuer das Outlook-Skript -- fremde Kontaktdaten
tools/*.local.txt
